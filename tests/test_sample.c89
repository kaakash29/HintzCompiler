struct Vec2 {
    int x;
    float y;
};

int foo(int in) {
    return in;
}

int main() {
    int a;
    float m[3];
    struct Vec2 v;
    int b;
    int c;
    int d;

    a = 2;
    b = 3;
    c = a + b;

    v.x = 11;
    a = m[0];

    m[0] = 1;

    d = foo(b);

    return c;
}
