# Hintz Compiler

## Install

From the repo root:

```bash
pip install .
```

If you want graph output (CFG/BBG), install Graphviz system binaries:

```bash
sudo apt-get install graphviz
```

## Usage

```bash
hintz -<options> somefile.hz
```
